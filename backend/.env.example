PORT=3000
PUBLIC_BASE_URL=http://localhost:3000

# 跨域白名单（逗号分隔，如 http://localhost:5173,https://example.com）；留空表示关闭 CORS（生产同源反代推荐）
CORS_ORIGIN=

# 内容目录（唯一数据源）
CONTENT_DIR=./content
# 静态资源目录（封面/视频），经 /static 暴露
STATIC_DIR=./static

# SQLite 数据库文件（持久化路径）
DB_PATH=./data/app.db
